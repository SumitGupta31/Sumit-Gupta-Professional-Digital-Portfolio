import type { Metadata } from 'next'; import './globals.css'; import { BackgroundProvider } from '@/components/background/BackgroundProvider'; import { DynamicBackground } from '@/components/background/DynamicBackground'; import { profile } from '@/data/profile';
export const metadata: Metadata = { title: `${profile.name} | MuleSoft Developer`, description: profile.summary, openGraph: { title: `${profile.name} | Integration Specialist`, description: profile.summary, type: 'website' }, twitter: { card: 'summary_large_image', title: `${profile.name} | MuleSoft Developer`, description: profile.summary } };
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en" suppressHydrationWarning><body><BackgroundProvider><DynamicBackground/>{children}</BackgroundProvider></body></html>}
